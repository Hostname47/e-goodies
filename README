# E-Goodies Project

A **collaborative** full-stack application built with **React** (frontend) and a wide range of backend technologies with a main entry point built in **Symfony**, and a fully containerized environment.

--

Stepbs to build the project:

1. clone the project
2. cd to project location
3. run: sudo chmox +x build.sh
4. ./build.sh